import styled from "styled-components";
import { motion } from "framer-motion";

export const Flashlight = styled(motion.div)`
    z-index: 0;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 512px;
`